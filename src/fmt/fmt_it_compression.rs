use crate::interface::Error;

pub fn decompress_8_bit(buf: &[u8], len: u32, it215: bool) -> Result<Vec<u8>, Error> {
    todo!()
}

pub fn decompress_16_bit(buf: &[u8], len: u32, it215: bool) -> Result<Vec<u8>, Error> {
    todo!()
}

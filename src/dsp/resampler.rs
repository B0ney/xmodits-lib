use crate::interface::Sample;
use dasp::signal::interpolate::Converter;

use super::SampleBuffer;

pub fn downsample(sample: &mut SampleBuffer, target: u32) {
    
    todo!()
}

pub fn upsample(sample: &mut SampleBuffer, target: u32) {
    
    todo!()
}